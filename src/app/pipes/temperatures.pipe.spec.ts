import { TemperaturesPipe } from './temperatures.pipe';

describe('TemperaturesPipe', () => {
  it('create an instance', () => {
    const pipe = new TemperaturesPipe();
    expect(pipe).toBeTruthy();
  });
});
